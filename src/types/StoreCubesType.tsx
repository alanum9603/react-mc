import type { PositionType } from "./PositionType"
import type { StoreTextureType } from "./StoreTextureType"

export type StoreCubesType = {
  id: string, 
  pos: PositionType, 
  texture: StoreTextureType
}